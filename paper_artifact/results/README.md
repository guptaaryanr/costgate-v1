# Artifact Results

Generated benchmark results are intentionally not committed here by default.

Regenerate them with:

```bash
python benchmarks/costregbench/run.py --out .costgate/costregbench
```

Archive selected generated summaries (`summary.csv`, `summary.md`) with release artifacts when needed.
